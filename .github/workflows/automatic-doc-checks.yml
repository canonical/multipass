#
name: Automatic doc checks

on:
  workflow_call:
  workflow_dispatch:
    # Manual trigger

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  documentation-checks:
    runs-on: ubuntu-latest
    steps:
      - name: “Intentionally fail for testing”
        run: |
          echo "🚨 Forcing failure (docs workflow) for testing purposes"
          exit 1
