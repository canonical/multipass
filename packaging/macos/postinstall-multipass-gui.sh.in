#!/bin/bash
set -e

### /usr/local/bin doesn't exist on a clean OSX install, yet it is in the default $PATH!
mkdir -p ${3}usr/local/bin

### Set up symlink to the multipass.gui binary
ln -fsv "${3}@CPACK_PACKAGING_INSTALL_PREFIX@/bin/multipass.gui" "${3}usr/local/bin/multipass.gui"

echo 0
