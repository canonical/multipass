[pytest]
markers =
    seed: Seed instances on the legacy version.
    verify: Verify instances with the upgraded version.
    scenario: Bind a seed/verify pair to an instance name.
    snapshot: Snapshot test supported only by snapshot-capable drivers.
