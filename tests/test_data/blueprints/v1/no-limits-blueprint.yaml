description: A Blueprint without limits
version: 0.1

instances:
  no-limits-blueprint:
    image: "default"
    timeout: 600
    cloud-init:
      vendor-data: |
        #cloud-config
        runcmd:
          echo "Have fun!"
