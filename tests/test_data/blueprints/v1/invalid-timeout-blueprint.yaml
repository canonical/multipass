description: Invalid timeout
version: 0.1

instances:
  invalid-timeout-blueprint:
    image: "default"
    limits:
      min-cpu: 2
      min-mem: 2G
      min-disk: 25G
    timeout:
    - Messed up
