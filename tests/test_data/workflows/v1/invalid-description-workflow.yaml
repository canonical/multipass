description:
  - Invalid description workflow
version: 0.1

instances:
  invalid-description-workflow:
    image: "default"
    limits:
      min-cpu: 2
      min-mem: 2G
      min-disk: 25G
    cloud-init:
      runcmd:
      - echo "Have fun!"
