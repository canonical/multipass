description: Missing version

instances:
  missing-version-workflow:
    image: "default"
    limits:
      min-cpu: 2
      min-mem: 2G
      min-disk: 25G
    cloud-init:
      runcmd:
      - echo "Have fun!"
