description: The first test workflow
version: 0.1

instances:
  test-workflow1:
    image: "default"
    limits:
      min-cpu: 2
      min-mem: 2G
      min-disk: 25G
    cloud-init:
      vendor-data: |
        #cloud-config
        runcmd:
          echo "Have fun!"
